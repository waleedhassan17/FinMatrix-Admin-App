/**
 * @deprecated Shim — the design system moved to src/theme/theme.ts
 * (single canonical theme module: import from '../theme' going forward).
 * Kept so the existing `utils/theme` imports keep resolving unchanged.
 */
export * from '../theme/theme';
export { default } from '../theme/theme';
